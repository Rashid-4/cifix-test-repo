name: Test New Patterns

on:
  push:
    branches: [main]

jobs:
  rust-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Rust project
        run: |
          echo "Compiling project..."
          echo "error: linker \`cc\` not found"
          echo "note: the linker is needed to link your binary"
          exit 1
